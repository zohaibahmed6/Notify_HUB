# .NET
bin/
obj/
*.user
.vs/

# EF Core secrets / local config
**/appsettings.Development.json
**/appsettings.*.local.json

# Node
node_modules/
notifyhub-web/dist/
notifyhub-web/.vite/
*.tsbuildinfo

# Env files
.env
.env.local
.env.*.local

# Test/coverage output
TestResults/
coverage/
# `coverage/` above inadvertently matched docs/coverage/ too, silently untracking the
# committed FR-013 doc (DOMAIN_COVERAGE.md) — re-include it explicitly. The generated
# report/raw dirs (coverage-report/, coverage-raw/) stay ignored below.
!docs/coverage/
coverage-report/
coverage-raw/
**/coverage.cobertura.xml
notifyhub-web/test-results/
notifyhub-web/playwright-report/
notifyhub-web/blob-report/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
*.swp
