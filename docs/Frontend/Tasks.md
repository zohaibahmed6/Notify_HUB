# Tasks — Frontend

Anchor file for the Tasks feature's frontend documentation, referenced from
`docs/DOCUMENT_INDEX.md`. No implementation detail is duplicated here — see:

- `CODEBASE_MAP.md` §6 (legacy `TaskBoardPage.tsx`/`NewTaskForm.tsx`/`TaskDetailPanel.tsx`) and
  §6a (v2 redesign: `TaskBoardPageV2`, `task-detail-sheet.tsx`).
- `CODEBASE_MAP.md` §6g (shared `DateTimePicker`, P9-03) and the P9-11 recurring-task UI notes
  in `CreateTaskForm.tsx`/`NewTaskForm.tsx`.
- `PROJECT_CONTEXT.md` FR-008, BR-007, BR-014 for the functional spec and business rules.

Update this file only when Tasks-frontend documentation needs to say something
`CODEBASE_MAP.md` doesn't already cover — otherwise just keep the cross-reference current.
