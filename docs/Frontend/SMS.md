# SMS — Frontend

Anchor file for the SMS feature's frontend documentation, referenced from
`docs/DOCUMENT_INDEX.md`. No implementation detail is duplicated here — see:

- `CODEBASE_MAP.md` §4b (Reminder SMS engine, P9-08) for `reminder-sms-dialog.tsx` and its
  `conversation-panel.tsx` toolbar entry point.
- `CODEBASE_MAP.md` §6h for the SMS History report.
- `CODEBASE_MAP.md` §6b/§6a for template preview/insertion in the composer
  (`ConversationPanelV2.handleInsertTemplate`, P9-04).
- `PROJECT_CONTEXT.md` FR-001–004, FR-009 for the functional spec.

Update this file only when SMS-frontend documentation needs to say something
`CODEBASE_MAP.md` doesn't already cover — otherwise just keep the cross-reference current.
